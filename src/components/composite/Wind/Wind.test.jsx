import { describe } from "vitest";

// todo: fix tests.

describe("Wind", () => {
  // it("renders a formatted data", async () => {
  //   const { findAllByTestId, debug } = setup();
  //   debug();
  //   expect(await findAllByTestId("list-date")[0].textContent).toBe(
  //     "Sunday Mar 17"
  //   );
  // });
  // it("renders a formatted temp", () => {
  //   const { getAllByTestId } = setup();
  //   expect(getAllByTestId("list-temp")[0].textContent).toBe("10°C");
  // });
  // it("renders a icon", () => {
  //   const { getAllByAltText } = setup();
  //   expect(getAllByAltText("sunrise icon")[0]).toBeInTheDocument();
  // });
});
